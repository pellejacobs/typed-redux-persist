{
  "private": true,
  "scripts": {
    "bundle": "typings bundle . -o bundle.d.ts",
    "test-spec": "ts-node test.ts",
    "test": "npm run bundle && npm run test-spec"
  },
  "devDependencies": {
    "redux-persist": "3.2.2",
    "ts-node": "^0.7.3",
    "tslint-config-typings": "^0.2.3",
    "typescript": "^1.8.10",
    "typings": "^1.0.4"
  }
}
